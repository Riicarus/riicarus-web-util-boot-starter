web:
  util:
    log-pointcut: execution(public * *..*.controller..*.*(..))
    jwt:
      secret: JWTTOKEN!@#$%
      expiration: 1000
      issuer: Riicarus
    file:
      store:
        paths:
          img: img
          pdf: pdf
      max-size: 10000
